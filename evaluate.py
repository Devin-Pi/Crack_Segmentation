# how to conduct evaluation between label and segmentation results?
import lightning as l
from PIL import Image
from torchmetrics.image import
def compute_mIoU(prediction, png_label):
    # prediction: 2D array, 0-255
    # png_label: 2D array, 0-255

    return mIoU




if __name__ == 'main':

    model = model.

    # how to compute the mIoU?

    # how to compute the accuracy?